os:
  editPreset: nvim

gui:
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline

git:
  paging:
    colorArg: never
    pager: delta --paging=never

quitOnTopLevelReturn: true
